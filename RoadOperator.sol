pragma solidity ^0.4.6;

contract RoadOperator {

    function contributeFees() {
        // Tracks the Vehicle 
        // Calculate fees for the Vehicle  
        // Vehicle pays the fee to the contract 
    }
    
    function withdrawFees() {
        // Calculate Total Fees in the Contract
        // Sends the Fees to the Regulator Contract 
    }
    
    function requestRefund() {
        // Vehicle can request a refund if a certain number of parameters are met 
        // Tracks the Vehicle 
    }
}